import React from "react";

import HomePageStyles from '../HomePage/HomePage.module.css';

const NotFoundPage = () => {
    return(
        <h1 className={HomePageStyles.title}>Not found :(</h1>
    );
};


export default NotFoundPage;